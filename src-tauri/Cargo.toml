[package]
name = "redliner"
version = "0.1.0"
description = "Fast media collection for working DJs"
authors = ["Redliner"]
edition = "2021"

[lib]
name = "redliner_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1", features = ["process", "io-util"] }
arboard = "3"
walkdir = "2"
trash = "5"
percent-encoding = "2"

[dev-dependencies]
tempfile = "3"
